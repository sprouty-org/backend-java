server:
  port: 8083
  forward-headers-strategy: framework

spring:
  application:
    name: sensor-service

springdoc:
  api-docs:
    path: /sensors/v3/api-docs
  swagger-ui:
    path: /sensors/swagger-ui.html
    operations-sorter: alpha


logbook:
  format:
    style: http
  filter:
    enabled: true
  predicate:
    exclude:
      - path: /actuator/**
      - path: /favicon.ico

logging:
  level:
    root: WARN
    org.springframework.web: INFO
    org.zalando.logbook.Logbook: TRACE

management:
  endpoints:
    web:
      base-path: /sensors/actuator
      exposure:
        include: "health,info,prometheus"
  endpoint:
    health:
      probes:
        enabled: true
      show-details: always
